@model HMS.Models.tblPatientTest

@{
    ViewBag.Title = "AddPatientTest";
}
@using (Html.BeginForm("AddPatientTest", "Patient", new { Patient_id = Request.QueryString["Patient_id"] }))
{

    @Html.ValidationSummary(true)
    <section class="content">
        <div class="container-fluid">
            <div class="block-header">
                <h2>Add Patient Test</h2>
            </div>
            <div class="row clearfix">
                <div class="col-lg-12 col-md-12 col-sm-12">
                    <div class="card">
                        <div class="header">
                            <h2>Basic Information</h2>

                        </div>
                        <div class="body">
                            <div class="row clearfix">
                                <div class="col-sm-3">
                                    <div class="form-group">
                                        <div class="form-group drop-custum">
                                            @Html.DropDownListFor(model => model.PatientAppointmentID, new SelectList(ViewBag.lstAppointment, "ID", "AppointmentDate"), "---Select Appointment---", new { @class = "form-control show-tick", @id = "ddlAppointmentDate" })



                                        </div>
                                    </div>
                                </div>
                                <div class="col-sm-3">
                                    <div class="form-group">
                                        <div class="form-group drop-custum">
                                            @Html.DropDownListFor(model => model.TestID, new SelectList(ViewBag.lstTest, "ID", "TestName"), "---Select Test---", new { @class = "form-control show-tick", @id = "ddltest" })



                                        </div>
                                    </div>
                                </div>
                                <div class="col-sm-6">
                                    <div class="form-group">
                                        <div class="form-line">
                                            @Html.TextAreaFor(x => x.PathalogistRemarks, new { @type = "Text", @class = "form-control no-resize", @id = "txtremars", @placeholder = "Pathalogist Remarks", @required = "required" })

                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="body table-responsive" id="testDetail">
                                </div>
                            </div>
                        </div>
                </div>

            </div>
            <div class="overlay">
                <div id="loading-img"></div>
                Loading...
            </div>
    </section>
    <script>
        $(document).ready(function () {
            debugger;
            $('#ddltest').change(function () {
                debugger;
                var id = $('#ddltest').find(":selected").val();
                if (id != 0 && id != "") {
                    debugger;
                    $.ajax({
                        url: "/GetTestDetails",
                        type: 'post',
                        contentType: 'application/json',
                        dataType: 'json',
                        data: '{"test_id":"' + id + '"}',

                  
                        success: function (data) {
                            debugger;
                            if (data.Success === "true") {
                                var lstDesignation = data.Data.Test;
                                if (lstDesignation.length > 0) {
                                    for (var i = 0; i < lstDesignation.length; i++) {
                                     
                                    }
                                }

                            }
                        },
                        error: function () {
                        },
                       
                    });
                }
            });
        });
        function showLoader() {
            $(".overlay").show();
        }
        function hideLoader() {
            $(".overlay").hide();
        }
    </script>
}